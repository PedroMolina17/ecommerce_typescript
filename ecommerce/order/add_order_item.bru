meta {
  name: add_order_item
  type: http
  seq: 2
}

post {
  url: {{BASE_URL}}/order/add-order-item
  body: json
  auth: none
}

body:json {
  {
    "orderId":2,
    "productId":4,
    "quantity":4,
    "unitPrice":300,
    "totalItemPrice":1200
  }
}
