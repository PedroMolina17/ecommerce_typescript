meta {
  name: login
  type: http
  seq: 1
}

post {
  url: {{BASE_URL}}/auth/login-admin
  body: json
  auth: none
}

body:json {
  {
    "email": "admin@example.com",
    "password": "password"
  }
}
